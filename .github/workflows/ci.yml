name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  # Headless Qt for tests (PySide6 / QML integration).
  QT_QPA_PLATFORM: offscreen
  QML_XHR_ALLOW_FILE_READ: "1"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Ruff
        run: |
          python -m pip install -U pip
          python -m pip install "ruff>=0.9"

      - name: Ruff (pyflakes)
        run: ruff check src tests

      - name: Byte-compile (syntax)
        run: python -m compileall -q src/texasholdemgym

  test-build:
    name: Test & build
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package + test deps
        run: |
          python -m pip install -U pip
          python -m pip install -e ".[test]"

      # PySide6 loads libEGL at import time; GitHub-hosted Ubuntu images omit it by default.
      - name: Install Qt headless system libs
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libgl1

      - name: Test (pytest + coverage report + fail_under)
        run: texas-holdem-gym-test

      - name: Build sdist and wheel
        run: |
          python -m pip install -U build
          python -m build

      - name: Upload distribution artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-ubuntu-py${{ matrix.python-version }}
          path: dist/*
